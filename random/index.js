const random = require('./random_int')
const randomArray = require('./random_array')

//console.log('my_module')
//console.log(random())

module.exports = {
    'randomNumber': random,
    'randomArray': randomArray
}